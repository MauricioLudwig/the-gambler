import React, { useEffect } from 'react';
import socketIOClient from 'socket.io-client';

const Home = () => {
  useEffect(() => {
  }, []);

  return (
    <div>
      Hello World!
    </div>
  );

};

export default Home;